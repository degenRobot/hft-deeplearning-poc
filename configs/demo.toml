# This lab only consumes public/replay market data and emits synthetic paper quotes.
source = "replay"
symbol = "BTCUSDT"
gate_mode = "neural" # neural, uniform, or static
higher_level_influence = 0.35
gate_interval_ms = 1000
expert_strength = 1.0
base_spread_bps = 2.0
max_inventory = 0.50
stale_after_ms = 2500
